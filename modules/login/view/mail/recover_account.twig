
<h1>Hola {{ user.nombre }}!</h1>
<p>Parece que has solicitado recuperar tu contraseña.</p>
<p>Para restablecer la contraseña haz click aqui en este enlace:</p>
<p><a href="{{ this.getLink(this, {token: recovery.token}, true) }}">{{ this.getLink(this, {token: recovery.token}, true) }}</a> </p>