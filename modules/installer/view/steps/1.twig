
<div class="row">
    <div class="col-lg-8">
    {% embed "views/templates/box.twig" %}
        {% block title %}
            Nombre
        {% endblock %}
        {% block content %}

            <form class="form-horizontal" method="post" action="{{ this.getLink(this, {step: this.step}) }}" enctype="multipart/form-data">
                <div class="form-group">
                    <label class="control-label col-lg-4">Nombre del sitio</label>
                    <div class="col-lg-8">
                        <input class="form-control" name="site_name" value="{{ mainconfig.app.name }}">
                    </div>
                </div>
                <div class="form-group">
                    <label class="control-label col-lg-4">Imagen de cabecera</label>
                    <div class="col-lg-8">
                        <input type="file" name="banner" class="form-control">
                        <p>
                            Imagen actual:
                            <img class="full-width" src="{{ mainconfig.app.banner }}">
                        </p>
                    </div>
                </div>
                <div class="form-group">
                    <div class="col-sm-offset-4 col-sm-8">
                        <div class="checkbox">
                            <label>
                                <input type="checkbox" name="debug" value="true" {{ mainconfig.app.debug?"checked":"" }}> Modo desarrollador activado
                            </label>
                        </div>
                    </div>
                </div>


                <div class="form-group">
                    <div class="col-lg-8 col-lg-offset-4">
                        <input type="submit" class="btn btn-success" name="save" value="Siguiente Paso">
                    </div>
                </div>
            </form>

        {% endblock %}
    {% endembed %}
    </div>
    <div class="col-lg-4">
        <h3>Ayuda</h3>
        <ul>
            <li><b>Nombre del sitio</b>: Este es el nombre que se mostrará en el título de tu sitio</li>
        </ul>
    </div>
</div>