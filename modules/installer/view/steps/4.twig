
<div class="row">
    <div class="col-lg-8">
    {% embed "views/templates/box.twig" %}
        {% block title %}
            Configuracion de la cuenta de administrador
        {% endblock %}
        {% block content %}
                <form class="form-horizontal" method="post" action="{{ this.getLink(this, {step: this.step}) }}">
                    {% if not this.hasAdmin %}
                    <div class="form-group">
                        <label class="control-label col-lg-4">Nombre de usuario</label>
                        <div class="col-lg-8">
                            <input class="form-control" pattern="[a-zA-Z0-9_-]+" name="username" value="{{ _POST.username }}">
                            <small>Solo se permite [a-zA-Z0-9_-]+</small>
                        </div>
                    </div>

                    <div class="form-group">
                        <label class="control-label col-lg-4">Contraseña</label>
                        <div class="col-lg-8">
                            <input class="form-control" name="password" type="password">
                        </div>
                    </div>

                    <div class="form-group">
                        <label class="control-label col-lg-4">Confirmar</label>
                        <div class="col-lg-8">
                            <input class="form-control" name="password_retype" type="password">
                        </div>
                    </div>
                    <div class="form-group">
                        <label class="control-label col-lg-4">Nombre completo</label>
                        <div class="col-lg-8">
                            <input class="form-control" name="nombre" value="{{ _POST.nombre }}">
                        </div>
                    </div>
                    <div class="form-group">
                        <label class="control-label col-lg-4">Email</label>
                        <div class="col-lg-8">
                            <input class="form-control" name="email" type="email" value="{{ _POST.email }}">
                        </div>
                    </div>

                    <div class="form-group">
                        <div class="col-lg-8 col-lg-offset-4">
                            <input type="submit" class="btn btn-success" name="save" value="Crear cuenta">
                        </div>
                    </div>
                {% else %}
                    <div class="alert alert-warning">
                        Ya existe una cuenta de aministrador
                    </div>

                    <div class="form-group">
                        <div class="col-lg-8 col-lg-offset-0">
                            <a href="{{ this.next() }}" class="btn btn-success">Siguiente</a>
                        </div>
                    </div>
                {% endif %}

                </form>

        {% endblock %}
    {% endembed %}
    </div>
    <div class="col-lg-4">
        <h3>Ayuda</h3>
        <ul>
            <li><b>Nombre de usuario</b>: Usa este usuario para logearte a la web.</li>
            <li><b>Contraseña</b>: Piensa en una contraseña fuerte.</li>
            <li><b>Confirmar</b>: Repite la misma contraseña que has puesto arriba.</li>
            <li><b>Nombre completo</b>: Utilizaremos este nombre para referirnos a ti.</li>
            <li><b>Email</b>: Email para comunicaciones privadas.</li>

        </ul>
    </div>
</div>