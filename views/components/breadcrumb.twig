<ol class="breadcrumb">
    <li><a href="/" target="_self"><i class="glyphicon glyphicon-home"></i> Home</a></li>
    {% for k, v in prev %}
        <li><a href="{{ v }}" target="_self">{{ k | raw }}</a></li>
    {% endfor %}
    <li>{{ current | raw }}</li>
</ol>
